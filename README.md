# PSE-Image

Como executar:

Crie um ambiente virtual
    python -m venv .venv

Ative o ambiente virtual
    .\.venv\Scripts\activate

Instale as bibliotecas necessárias:
    pip install PySide6 numpy qimage2ndarray

Execute o programa:
    python main.py